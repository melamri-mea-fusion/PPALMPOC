name: CI + CD

on:
  pull_request:
    branches: [ main ]
    
jobs:      
  DeployUAT:
    name: Deploy to UAT 
    runs-on: windows-latest
    environment: 
      name: UAT
    steps:
      - name: Deploy
        uses: ./.github/workflows/MigrateSPLists.yml
            
  DeployProd:
    name: Deploy to Production 
    needs: [DeployUAT]
    runs-on: windows-latest
    environment: 
      name: PROD
    steps:
    - name: Deploy
      uses: ./.github/workflows/MigrateSPLists.yml
    
