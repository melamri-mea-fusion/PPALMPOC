name: Release action
# Call the reusable workflow release-solution-with-inputs.yml
# Release your solution to prod when you create a new release.

on:
  release:
      types: [created]        
permissions:
  contents: write
jobs:
  Release-solution-ALMLab:
    uses: ./.github/workflows/release-solution-to-prod-with-inputs.yml
    with:
      #You can specify the solution name here
      solution_name: PPALMPOC
      #Update your values here
      BUILD_ENVIRONMENT_URL: 'https://org158aa323.crm4.dynamics.com'
      PRODUCTION_ENVIRONMENT_URL: 'https://org005dbe5b.crm4.dynamics.com'
      CLIENT_ID: '4bf59db1-3f93-48dc-a75c-d2a2e9a6ed75'
      TENANT_ID: 'd57f6302-e7b3-487f-b17a-5f7f14c6228e'   
    secrets:
      envSecret: ${{ secrets.SPN }}
